snapshots:
    - name: goldItems
      relation: ref('producedItems')
      config:
        schema: produced
        database: '{{ target.catalog }}' 
        unique_key: id
        strategy: timestamp
        updated_at: updatedDate
        dbt_valid_to_current: "to_date('9999-12-31')"